<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order - Dream House Furniture</title>
    <style>
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            background-color: #f4f4f4;
        }
        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
        }

        /* Header styles */
        header {
            background-color: #fff;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .logo {
            width: 200px;
            padding: 10px 0;
        }

        /* Shop page styles */
        .shop-container {
            display: flex;
            gap: 30px;
            padding: 30px 0;
        }
        .sidebar {
            width: 250px;
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        .sidebar h3 {
            margin-bottom: 15px;
            font-size: 18px;
        }
        .filter-group {
            margin-bottom: 20px;
        }
        .filter-group label {
            display: block;
            margin-bottom: 5px;
        }
        .filter-group input[type="checkbox"] {
            margin-right: 5px;
        }
        .products-grid {
            flex: 1;
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
        }
        .product-card {
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            overflow: hidden;
            transition: transform 0.3s ease;
            display: flex;
            flex-direction: column;
        }
        .product-card:hover {
            transform: translateY(-5px);
        }
        .product-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        .product-info {
            padding: 15px;
            display: flex;
            flex-direction: column;
            flex-grow: 1;
        }
        .product-title {
            font-size: 18px;
            margin-bottom: 10px;
        }
        .product-description {
            font-size: 14px;
            color: #666;
            margin-bottom: 10px;
        }
        .product-price {
            font-size: 18px;
            font-weight: bold;
            color: #8CC63F;
            margin-bottom: 10px;
        }
        .order-address {
            background-color: #f8f8f8;
            border: 1px solid #e7e7e7;
            border-radius: 4px;
            padding: 10px;
            margin-top: 10px;
            font-size: 0.9em;
        }
        .order-address h3 {
            margin-top: 0;
            margin-bottom: 10px;
            font-size: 1.1em;
            color: #333;
        }
        .order-address p {
            margin: 5px 0;
        }
        .btn-danger {
            margin-top: 10px;
            align-self: flex-start;
        }

        /* Responsive styles */
        @media (max-width: 768px) {
            .shop-container {
                flex-direction: column;
            }
            .sidebar {
                width: 100%;
                margin-bottom: 20px;
            }
        }
    </style>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<body>

    <main class="container shop-container">
        <div class="products-grid">
            <% if (!(products.length===0)) { %>
                <% products.forEach(function(product) { %>
                    <div class="product-card">
                        <img src="<%= product.picture %>" alt="<%= product.title %>" class="product-image">
                        <div class="product-info">
                            <h2 class="product-title"><%= product.title %></h2>
                            <p class="product-description"><%= product.description %></p>
                            <p class="product-price">Total Amount : Rs. <%= product.price %></p>
                            <div class="order-address">
                                <h3>Delivery Address:</h3>
                                <p><strong>Name:</strong> <%= product.address.fullName %></p>
                                <p><strong>Street:</strong> <%= product.address.street %></p>
                                <p><strong>City:</strong> <%= product.address.city %></p>
                                <p><strong>Postal Code:</strong> <%= product.address.postalCode %></p>
                            </div>
                            <p class="product-date">Date : <%= (product.date).toLocaleDateString('en-GB'); %></p>
                            <a href="/delete_Order/<%=product._id %>">
                                <button type="button" class="btn btn-danger">Cancel Order</button>
                            </a>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <p class="product-description">Sorry! No product Found</p>
            <% } %>
        </div>
    </main>
    
</body>
</html>

